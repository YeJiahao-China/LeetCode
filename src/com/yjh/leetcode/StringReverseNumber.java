package com.yjh.leetcode;

public class StringReverseNumber {

	public static void main(String[] args) {

	}

	/*public static int myAtoi(String str) {
		// 如果字符串的首位字符不是‘+’或者‘-’ 返回 0
		if (str.charAt(0) != '+' && str.charAt(0) != '-') {
			return 0;
		}
		// 对字符串转换成的数字是正数还是负数进行判断
		// flag == 0表示是带+符号的正数 flag==1表示是负数 2表示第一位不是符号
		int flag = 2;
		if (str.charAt(0) == '-') {
			flag = 1;
		}
		if (str.charAt(0) == '+') {
			flag = 0;
		}
		int i = 0;
		if (flag == 1 || flag == 0) {
			i = 1;
		}
		
	}*/
}
